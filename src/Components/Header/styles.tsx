import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    display: "flex",
    justifyContent: "flex-end",
    alignContent: "center",
    height: "50px",
    background: "#008bfc",
  },
  button: {
    borderRadius: "50%",
  },
  icon: {
    color: "white",
  },
});
